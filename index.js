import React from 'react';
import ReactDOM from 'react-dom';
import App from './src/client/app.jsx';

// create react node on HTML DOM and renders it
ReactDOM.render(
  <App />,
  document.getElementById('root'),
);
